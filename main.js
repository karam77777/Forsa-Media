/* ============================================
   TRANSLATION DICTIONARY
   ============================================ */
const translations = {
  ar: {
    // Navigation
    "nav-home": "الرئيسية",
    "nav-services": "خدماتنا",
    "nav-mascot": "ريشو",
    "nav-why": "مميزاتنا",
    "nav-portfolio": "أعمالنا",
    "nav-process": "طريقة العمل",
    "nav-packages": "عروضنا",
    "nav-faq": "الأسئلة الشائعة",
    "nav-contact": "تواصل معنا",
    "btn-consult": "ابعتلنا استفسارك",

    // Hero Section
    "audio-widget-label": "لو معندكش وقت تسكرول في الموقع اعرف احنا بنقدم اية من هنا",
    "audio-portfolio-btn": "أعمالنا",
    "hero-badge": "فرصة ميديا",
    "hero-title-1": "نحو",
    "hero-accent": "فرصة ..",
    "hero-title-2": "تصنع الفرق",
    "hero-sub": "بنساعد شركتك تكبر وتتميّز عن غيرها، من خلال تصميم احترافي، إدارة محتوى، إعلانات ممولة، وطباعة وبخبرة فريق متكامل فاهم سوقك كويس.",
    "hero-btn-start": "اطلب عرض سعر",
    "hero-btn-portfolio": "تواصل معنا",
    "stat-happy-clients": "عملائنا",
    "stat-projects": "مشروع مكتمل",
    "stat-roi": "سنوات خبرة",

    // Services Section
    "services-badge": "ماذا نقدّم",
    "services-title": "خدمات تصنع الفرق",
    "services-desc": "من الفكرة إلى التنفيذ، نقدم حلولاً إبداعية متكاملة تساعد مشروعك على النمو والتميز في السوق.",
    "srv-design-title": "التصميم الجرافيكي",
    "srv-design-desc": "من الشعار والألوان والخطوط، لحد المطبوعات والتصاميم الرقمية وتعديل الصور — بنبني شكل بصري متكامل لعلامتك يخليها متعرفة من أول نظرة.",
    "srv-print-title": "الطباعة والدعاية",
    "srv-print-tag": "مراجعة فنية قبل الطباعة",
    "srv-print-desc": "من البطاقات والبروشورات والمجلات لحد اللوحات الخارجية — بنراجع كل ملف فني قبل الطباعة عشان نضمن مفيش خطأ يكلفك فلوس زيادة.",
    "srv-social-title": "إدارة صفحات السوشيال ميديا",
    "srv-social-desc": "خطة محتوى ذكية، نشر منتظم، وتفاعل حقيقي بيحول متابعينك لعملاء دايمين على كل المنصات.",
    "srv-ads-title": "الإعلانات الممولة",
    "srv-ads-desc": "حملات ممولة مدروسة على فيسبوك وإنستجرام وتيك توك. بتوصلك لعميلك الحقيقي وتزود مبيعاتك، وتضمن لك تحقيق أعلى عائد.",
    "srv-more": "اطلب الخدمة",
    "srv-pkg-title": "تصميم العبوات والمنتجات",
    "srv-pkg-desc": "تصميم عبوات وكراتين وبوكسات لمنتجاتك، بشكل عملي وجذاب يخلي منتجك ياخد عينه أول ما يتحط على الرف أو يتصور.",
    "srv-edit-title": "المونتاج",
    "srv-edit-desc": "مونتاج احترافي للريلز والفيديوهات القصيرة، بمؤثرات بصرية وموشن جرافيك ونصوص متحركة، لمحتوى يوقف السكرول.",

    // Mascot Section
    "mascot-badge": "تميمتنا الرسمية",
    "mascot-name": "تعرّف على مساعدك الإبداعي",
    "mascot-title": "ريشو — عبقري فرصة ميديا",
    "mascot-desc": "ذكي وسريع وصاحب حس تسويقي عالٍ، يرافقك في كل خطوة من رحلتك مع فرصة ميديا. من كتابة المحتوى إلى تصميم منشوراتك وإطلاق حملاتك، ريشو هنا ليجعل التسويق أسهل وأكثر متعة.",
    "trait-creative": "إبداعي",
    "trait-fast": "سريع الخطى",
    "trait-smart": "ذكي ومرن",
    "trait-support": "ودود وداعم",
    "trait-fun": "مرح",
    "role-guide": "دليلك الإبداعي",
    "role-icon": "رمز الهوية البصرية",
    "role-mascot-label": "تميمة فرصة ميديا الرسمية",
    "role-content": "كتابة المحتوى",
    "role-design": "تصميم",
    "role-social": "إدارة الصفحات",
    "role-launch": "إطلاق الحملات",
    "role-print": "خدمات الطباعة",
    "role-pkg": "تصميم العبوات",

    // Why Choose Us
    "why-badge": "لماذا نحن؟",
    "why-title": "شريك تسويقي تقدر تعتمد عليه",
    "why-sub": "بنجمع بين الإبداع والدقة والسرعة، عشان نديك تجربة تسويقية مختلفة، ونتائج بتتكلم عن نفسها.",
    "why-card-1-title": "تصميم إبداعي",
    "why-card-1-desc": "أفكار بصرية مميزة تخلي علامتك تبان وسط المنافسين.",
    "why-card-2-title": "تسليم سريع",
    "why-card-2-desc": "بنلتزم بالمواعيد من غير ما نضحي بجودة الشغل.",
    "why-card-3-title": "فريق محترف",
    "why-card-3-desc": "مصممين ومسوقين متخصصين فاهمين السوق كويس.",
    "why-card-4-title": "نتائج ملموسة",
    "why-card-4-desc": "بنقيس نجاحنا بنجاح شغلك ونمو مبيعاتك الحقيقي.",
    "why-card-5-title": "أسعار تنافسية",
    "why-card-5-desc": "باقات مرنة تناسب ميزانية مشروعك سواء لسه بادئ أو كبير.",
    "why-card-6-title": "دعم مستمر",
    "why-card-6-desc": "بنفضل معاك بعد التسليم لضمان أفضل أداء لحملاتك.",
    "why-stat-experience": "سنوات خبرة",
    "why-stat-designs": "تصميم مبتكر",
    "why-stat-print": "مطبوعات تم تسليمها",
    "why-stat-retention": "نسبة رضا العملاء",

    // Portfolio
    "pf-badge": "معرض أعمالنا",
    "pf-title": "أعمال تتحدث بالنيابة عنا",
    "pf-sub": "نماذج من مشاريعنا في التصميم، الطباعة، الهوية البصرية، والحملات الإعلانية.",
    "pf-btn-all": "الكل",
    "pf-btn-design": "تصميم الجرافيك",
    "pf-btn-print": "تصاميم مطبوعة",
    "pf-btn-social": "تصاميم سوشيال ميديا",
    "pf-btn-ads": "حملات إعلانية",
    "full-zoom-hint": "🔍 انقر على الصورة للتكبير والتمرير (Zoom & Pan)",
    "pf-btn-videos": "فيديوهات وريلز",

    // Process
    "proc-badge": "طريقة العمل",
    "proc-title": "من الفكرة إلى الانطلاق",
    "proc-sub": "عملية واضحة ومنظمة تضمن لك نتيجة تستحق وقتك واستثمارك.",
    "nav-toggle-aria": "فتح/إغلاق القائمة",
    "hero-chip-1": "تصميم إبداعي",
    "hero-chip-2": "جودة طباعة",
    "hero-chip-3": "تسليم سريع",
    "hero-chip-4": "إدارة الصفحات",
    "hero-chip-5": "مونتاج فيديوهات",
    "hero-chip-6": "إعلانات ممولة",
    "hero-chip-7": "تصميم عبوات",
    "hero-scroll": "مرر لأسفل",
    "proc-step-1-title": "فهم العميل",
    "proc-step-1-desc": "نستمع لأهدافك ونفهم طبيعة مشروعك.",
    "proc-step-2-title": "الاستراتيجية",
    "proc-step-2-desc": "نضع خطة تسويقية مدروسة تناسب سوقك.",
    "proc-step-3-title": "التصميم",
    "proc-step-3-desc": "نحوّل الاستراتيجية إلى تصاميم ملموسة.",
    "proc-step-4-title": "المراجعة",
    "proc-step-4-desc": "نراجع معك التفاصيل حتى الرضا التام.",
    "proc-step-5-title": "الانطلاق",
    "proc-step-5-desc": "ننشر وننفذ الحملة أو المشروع كاملاً.",
    "proc-step-6-title": "التحسين",
    "proc-step-6-desc": "نتابع الأداء ونطور النتائج باستمرار.",

    // Testimonials
    "testi-badge": "آراء عملائنا",
    "testi-title": "ثقة عملائنا أكبر إنجاز لنا",
    "testi-quote-1": "\"فرصة ميديا غيّروا شكل منصاتي بالكامل. من أول الهوية البصرية لحد مونتاج الريلز والتواصل سريع، ونتائج الاعلانات الاورجانيك كويسة جدا.\"",
    "testi-avatar-1": "ع",
    "testi-name-1": "أ/ عمر عزازي",
    "testi-role-1": "محلل مالي / سوقي",
    "testi-quote-2": "\"تسلم ايديكم هظبط الصفحة وإن شاء الله فيه شغل تاني رسوم متحركة ومونتاج .. تشرفت بيكم وإن شاء الله نكمل مع بعض\"",
    "testi-avatar-2": "م",
    "testi-name-2": "أ/ محمد حافظ",
    "testi-role-2": "محلل بيانات أسواق مالية",
    "testi-quote-3": "\"تعاملنا مع فرصة ميديا في طباعة وتجهيز مستندات الشركة والمواد الدعائية أكثر من مرة، وبصراحة الالتزام بالمواعيد وجودة الخامات والألوان كانت ممتازة جداً وبتعكس احترافية حقيقية.\"",
    "testi-avatar-3": "ع",
    "testi-name-3": "عربية أون لاين (NI Capital)",
    "testi-role-3": "شركة وساطة مالية",
    "testi-quote-4": "\"نسأل الله العلي القدير أن يبارك في جهود فريق فرصة ميديا؛ فقد قدموا لنا تصاميم مميزة لمنصاتنا الرقمية، ومونتاجاً متقناً لبودكاست الأكاديمية جمع بين الرقي البصري والوضوح السمعي، فكانوا نعم المعين في مسيرة البناء والنشر.\"",
    "testi-avatar-4": "و",
    "testi-name-4": "أكاديمية وحي",
    "testi-role-4": "منصة تعليمية شرعية",
    "testi-quote-5": "\"خدمة رائعة وتجاوب سريع من فريق العمل، ساعدونا كتير في إخراج منشورات وحملات الجمعية على السوشيال ميديا بشكل إنساني وجذاب يلمس قلوب المتبرعين والمستفيدين.\"",
    "testi-avatar-5": "ج",
    "testi-name-5": "جمعية أمة الحبيب",
    "testi-role-5": "للمساعدات الاجتماعية",
    "testi-quote-6": "\"تصميمات السوشيال ميديا اللي عملتوها لشركتنا ساهمت بشكل كبير في توضيح مزايا أنظمتنا الأمنية وعرضها بطريقة احترافية ومقنعة للعملاء، شغل ممتاز وتواصل راقي.\"",
    "testi-avatar-6": "س",
    "testi-name-6": "شركة سمارتيفيا (Smartivia)",
    "testi-role-6": "أجهزة الأمان والكاميرات",
    "testi-quote-7": "\"شغل المطبوعات مع فريق فرصة ميديا كان تجربة ممتازة، طلبنا مطبوعات بأشكال وخامات متنوعة وكان التنفيذ قمة في الدقة والسرعة مع تنسيق ألوان احترافي.\"",
    "testi-avatar-7": "ن",
    "testi-name-7": "أ/ نهاد علي",
    "testi-role-7": "مديرة منصة البورصجية للحلول المالية",
    "testi-quote-8": "\"صمموا لنا هوية بصرية متكاملة للشركة ونفذوا لنا كل المطبوعات الرسمية والدعائية كذا مرة، شغلهم مميز ويفهموا السوق وبيضيفوا لمسة احترافية قوية للهوية.\"",
    "testi-avatar-8": "إ",
    "testi-name-8": "شركة إيجي كيم (EgyChem)",
    "testi-role-8": "لتجارة خامات البلاستيك",
    "testi-quote-9": "\"تواصل سريع وتنفيذ متقن ومطابق للمواصفات اللي طلبناها في مطبوعات الشركة، شكرًا لفريق فرصة ميديا على التزامهم بالدقة والجودة العالية.\"",
    "testi-avatar-9": "ط",
    "testi-name-9": "أ/ محمد",
    "testi-role-9": "مدير شركة طاهر ستيل",
    "testi-quote-10": "\"التعامل معكم وفر علينا مجهود كبير، من تصميم الباكيدج والمينيو لحد الطباعة النهائية بجودة ممتازة وسرعة تسليم مدهشة، إن شاء الله تعاملنا مستمر.\"",
    "testi-avatar-10": "ك",
    "testi-name-10": "أ/ كريم",
    "testi-role-10": "مدير شركة توريدات عمومية",
    "testi-quote-11": "\"شكل وتصميم الباكيدج (العلب والأكياس) فرق جداً مع زباين المطعم، جودة الطباعة والألوان كانت مشرفة وجذابة، تسلم إيديكم على هذا المجهود الرائع.\"",
    "testi-avatar-11": "م",
    "testi-name-11": "أ/ محمد",
    "testi-role-11": "مدير مطعم 999",
    "testi-prev": "السابق",
    "testi-next": "التالي",

    // Packages
    "pkg-badge": "عروضنا",
    "pkg-title": "باقة تناسب حجم طموحك",
    "pkg-sub": "اختر الباقة المناسبة لمشروعك، أو تواصل معنا لتصميم باقة خاصة بك.",
    "pkg-basic": "الباقة الأساسية",
    "pkg-advanced": "الباقة المتقدمة",
    "pkg-pro": "الباقة الاحترافية",
    "pkg-price-period": "ج.م / شهرياً",
    "pkg-btn": "ابدأ الآن",
    "pkg-badge-popular": "الأكثر طلباً",
    "pkg-basic-desc": "مناسبة للمشاريع الناشئة التي تبدأ حضورها الرقمي.",
    "pkg-feat-basic-1": "12 تصميم سوشيال ميديا احترافي.",
    "pkg-feat-basic-2": "إدارة منصة واحدة.",
    "pkg-feat-basic-3": "كتابة الكابشن.",
    "pkg-feat-basic-4": "نشر المحتوى.",
    "pkg-feat-basic-5": "تصميم 4 ستوري.",
    "pkg-feat-basic-6": "إدارة إعلان ممول واحد (ميزانية الإعلان لا تشمل سعر الباقة).",
    "pkg-feat-basic-7": "تقرير أداء شهري.",
    "pkg-feat-basic-8": "دعم عبر واتساب.",
    "pkg-adv-desc": "للشركات التي تريد نمواً ثابتاً وحضوراً قوياً.",
    "pkg-feat-adv-1": "20 تصميم سوشيال ميديا.",
    "pkg-feat-adv-2": "إدارة حتى 3 منصات.",
    "pkg-feat-adv-3": "كتابة المحتوى والكابشن.",
    "pkg-feat-adv-4": "تصميم 8 Stories.",
    "pkg-feat-adv-5": "إدارة حملتين إعلانيتين.",
    "pkg-feat-adv-6": "متابعة الرسائل والتعليقات.",
    "pkg-feat-adv-7": "تقرير أداء أسبوعي.",
    "pkg-feat-adv-8": "تحسين الهوية البصرية للمحتوى.",
    "pkg-feat-adv-9": "أولوية في التنفيذ.",
    "pkg-pro-desc": "حلول متكاملة للشركات الكبيرة وعلامات التوسع.",
    "pkg-feat-pro-1": "30 تصميم سوشيال ميديا. (ضمن سياسة الاستخدام العادل).",
    "pkg-feat-pro-2": "إدارة جميع منصات التواصل.",
    "pkg-feat-pro-3": "كتابة المحتوى بالكامل.",
    "pkg-feat-pro-4": "تصميم Stories وReels Covers.",
    "pkg-feat-pro-5": "إدارة الحملات الإعلانية بشكل مستمر.",
    "pkg-feat-pro-6": "تحليل المنافسين.",
    "pkg-feat-pro-7": "خطة محتوى شهرية.",
    "pkg-feat-pro-8": "مدير حساب مخصص.",
    "pkg-feat-pro-9": "اجتماع متابعة شهري.",
    "pkg-feat-pro-10": "خصومات على خدمات التصميم والطباعة.",

    // Additional Services
    "extra-title": "خدمات إضافية (حسب الطلب)",
    "extra-sub": "تقدر تطلب أي خدمة من دول بشكل مستقل لتطوير مشروعك بدون الالتزام بباقة كاملة.",
    "extra-item-1-title": "تصميم الهوية البصرية",
    "extra-item-1-desc": "شعار، ألوان، خطوط، وتطبيقات متكاملة تمنح علامتك شخصية مميزة.",
    "extra-item-1-price": "يبدأ من 1,500 ج.م",
    "extra-item-2-title": "مونتاج فيديوهات ريلز",
    "extra-item-2-desc": "مونتاج ريلز وتيك توك احترافي شامل المؤثرات والموسيقى وتعديل الألوان.",
    "extra-item-2-price": "250 ج.م / للفيديو",
    "extra-item-3-title": "إدارة منصة إضافية",
    "extra-item-3-desc": "إدارة منصة إضافية تابعة لنفس نشاطك التجاري لزيادة انتشارك.",
    "extra-item-3-price": "500 ج.م / شهرياً",
    "extra-item-4-title": "إعلان ممول إضافي",
    "extra-item-4-desc": "إنشاء وإدارة حملة ترويجية إضافية مستهدفة لزيادة المبيعات.",
    "extra-item-4-price": "400 ج.م / شهرياً",
    "extra-item-5-title": "كتابة محتوى وتعديل صور",
    "extra-item-5-desc": "كتابة نصوص بوستات متوافقة مع علامتك وتعديل صور وبوسترات لمنصاتك.",
    "extra-item-5-price": "600 ج.م / شهرياً",
    "extra-item-6-title": "تصميم لوجو منفرد",
    "extra-item-6-desc": "تصميم شعار احترافي لعلامتك التجارية ليكون أساس هويتك البصرية.",
    "extra-item-6-price": "750 ج.م",

    // FAQ
    "faq-badge": "الأسئلة الشائعة",
    "faq-title": "عندك سؤال؟ غالباً عندنا الإجابة",
    "faq-q-1": "كم تستغرق مدة تنفيذ التصميم؟",
    "faq-a-1": "تختلف المدة حسب نوع المشروع، لكن التصاميم البسيطة تُسلّم خلال 2-3 أيام عمل، أما الهوية البصرية الكاملة فقد تستغرق أسبوعاً إلى أسبوعين.",
    "faq-q-2": "هل تقدمون خدمة الإعلانات الممولة لجميع المنصات؟",
    "faq-a-2": "نعم، نقدم حملات ممولة مدروسة على فيسبوك، وإنستجرام، وتيك توك، لتحقيق أقصى وصول وأفضل عائد.",
    "faq-q-3": "هل تشمل خدماتكم الطباعة والتسليم الفعلي؟",
    "faq-a-3": "نغطي جميع احتياجاتك الطباعية من التصميم حتى التنفيذ والتوصيل إلى باب موقعك.",
    "faq-q-4": "كيف يتم تحديد سعر المشروع؟",
    "faq-a-4": "يعتمد السعر على نوع الخدمة وحجم المشروع ومدته. نوفر باقات شهرية ثابتة، أو عروض سعر مخصصة للمشاريع لمرة واحدة.",
    "faq-q-5": "هل يمكن تعديل التصميم بعد التسليم؟",
    "faq-a-5": "نعم، كل باقاتنا تشمل حتى 3 تعديلات مجانية لضمان أفضل نتيجة، وأي تعديلات إضافية تكون بتكلفة إضافية بسيطة.",
    "faq-q-6": "إزاي بتضمنوا جودة الألوان والطباعة؟",
    "faq-a-6": "بنراجع كل ملف تصميم فنيًا بالكامل قبل ما نبعته للمطبعة، عشان نتأكد من صيغة الألوان (CMYK)، والمقاسات، وهوامش القص، عشان تتفادى أي غلطة في الطباعة.",

    // Contact
    "contact-badge": "تواصل معنا",
    "contact-title": "نحب نسمع صوتك",
    "contact-sub": "املأ النموذج وسيب الباقي علينا، وهنتواصل معاك في أسرع وقت. أو كلمنا مباشرة من القنوات اللي تحت 👇.",
    "field-name": "الاسم",
    "field-phone": "رقم الهاتف",
    "field-email": "البريد الإلكتروني",
    "field-biz": "نوع النشاط التجاري",
    "field-details": "تفاصيل الطلب",
    "placeholder-name": "مثال: أحمد سيد",
    "placeholder-msg": "أخبرنا أكثر عن مشروعك وااحتياجك...",
    "opt-restaurant": "مطعم أو كافيه",
    "opt-clinic": "عيادة أو مركز",
    "opt-education": "مؤسسة تعليمية",
    "opt-realestate": "شركة عقارية",
    "opt-ecommerce": "متجر إلكتروني",
    "opt-company": "شركة / مؤسسة",
    "opt-other": "أخرى",
    "form-btn-send": "إرسال الطلب عبر الواتساب",
    "form-note": "سيتواصل معك فريقنا خلال 24 ساعة عمل.",
    "card-phone-title": "اتصل بنا",
    "card-email-title": "راسلنا",
    "card-map-title": "موقعنا",
    "card-map-desc": "شارع المعتمدية بعد نزله دائري الكهرباء - أرض اللواء - الجيزة - مصر",
    "social-whatsapp": "واتساب",
    "social-facebook": "فيسبوك",
    "social-instagram": "انستغرام",
    "map-pin-text": "الجيزة - مصر",

    // Footer
    "footer-brand-desc": "فرصة ميديا: متخصصة في التصميم الجرافيكي، الطباعة، إدارة السوشيال ميديا، والإعلانات الممولة. نحو فرصة .. تصنع الفرق.",
    "footer-links-title": "روابط سريعة",
    "footer-contact-title": "تواصل معنا",
    "footer-contact-desc": "الجيزة - مصر",
    "footer-copy": "© 2026 فرصة ميديا. جميع الحقوق محفوظة.",
    "footer-by": "💚 صُمم بـواسطة فريق فرصة ميديا",
    "footer-services-title": "الخدمات",
    "footer-service-identity": "الهوية البصرية",
    "footer-service-print": "الدعاية والطباعة",
    "footer-service-social": "إدارة الصفحات",
    "footer-service-ads": "الحملات الممولة"
  },
  en: {
    // Navigation
    "nav-home": "Home",
    "nav-services": "Services",
    "nav-mascot": "Risho",
    "nav-why": "Why Us",
    "nav-portfolio": "Portfolio",
    "nav-process": "How We Work",
    "nav-packages": "Packages",
    "nav-faq": "FAQ",
    "nav-contact": "Contact Us",
    "btn-consult": "Send Us Your Inquiry",

    // Hero Section
    "audio-widget-label": "Don't have time to scroll? Find out what we offer here",
    "audio-portfolio-btn": "Our Work",
    "hero-badge": "Forsa Media",
    "hero-title-1": "Towards a",
    "hero-accent": "Chance ..",
    "hero-title-2": "That Makes a Difference",
    "hero-sub": "We help your company grow and stand out through professional design, content management, sponsored ads, and printing — backed by an integrated team that understands your market well.",
    "hero-btn-start": "Get a Quote",
    "hero-btn-portfolio": "Contact Us",
    "stat-happy-clients": "Our Clients",
    "stat-projects": "Completed Projects",
    "stat-roi": "Years of Experience",

    // Services Section
    "services-badge": "Our Services",
    "services-title": "Everything you need to grow your business in one place",
    "services-desc": "We provide a comprehensive package of digital and printing services to help your brand appear in the best way online and offline.",
    "srv-design-title": "Graphic Design",
    "srv-design-desc": "From logo, colors, and fonts, to prints, digital designs, and photo editing - we build a complete visual identity for your brand that gets recognized at first glance.",
    "srv-print-title": "Printing & Advertising",
    "srv-print-tag": "Pre-flight technical check",
    "srv-print-desc": "From cards, brochures, and magazines to outdoor signs - we review every artwork file before printing to ensure there are no costly errors.",
    "srv-social-title": "Social Media Management",
    "srv-social-desc": "Smart content plans, consistent posting, and real engagement that turns followers into loyal customers across all platforms.",
    "srv-ads-title": "Paid Advertising",
    "srv-ads-desc": "Targeted paid campaigns on Facebook, Instagram, and TikTok that reach your ideal customers, increase your sales, and ensure the ROI.",
    "srv-more": "Order Service",
    "srv-pkg-title": "Packaging & Product Design",
    "srv-pkg-desc": "Practical and attractive packaging, box, and carton designs that make your product stand out as soon as it is placed on the shelf or photographed.",
    "srv-edit-title": "Video Editing",
    "srv-edit-desc": "Professional video editing for Reels and short videos, with visual effects, motion graphics, and animated text, to create scroll-stopping content.",

    // Mascot Section
    "mascot-badge": "Official Mascot",
    "mascot-name": "Meet your creative assistant",
    "mascot-title": "Risho — Forsa Media Genius",
    "mascot-desc": "Smart, fast, and highly marketing-savvy, he accompanies you every step of your journey with Forsa Media. From content writing to designing your posts and launching your campaigns, Risho is here to make marketing easier and more fun.",
    "trait-creative": "Creative",
    "trait-fast": "Fast-paced",
    "trait-smart": "Smart & Agile",
    "trait-support": "Friendly & Supportive",
    "trait-fun": "Fun",
    "role-guide": "Your Creative Guide",
    "role-icon": "Visual Identity Icon",
    "role-mascot-label": "Official Forsa Media Mascot",
    "role-content": "Content Writing",
    "role-design": "Design",
    "role-social": "Page Management",
    "role-launch": "Launching Campaigns",
    "role-print": "Printing Services",
    "role-pkg": "Packaging Design",

    // Why Choose Us
    "why-badge": "Why Choose Us?",
    "why-title": "A marketing partner you can rely on",
    "why-sub": "We combine creativity, precision, and speed to give you a unique marketing experience, with results that speak for themselves.",
    "why-card-1-title": "Creative Design",
    "why-card-1-desc": "Distinctive visual ideas that make your brand stand out among competitors.",
    "why-card-2-title": "Fast Delivery",
    "why-card-2-desc": "We deliver on time without compromising on the quality of work.",
    "why-card-3-title": "Professional Team",
    "why-card-3-desc": "Specialized designers and marketers who understand the market well.",
    "why-card-4-title": "Tangible Results",
    "why-card-4-desc": "We measure our success by the success of your business and your real sales growth.",
    "why-card-5-title": "Competitive Prices",
    "why-card-5-desc": "Flexible packages that suit your project budget, whether you are starting out or growing.",
    "why-card-6-title": "Continuous Support",
    "why-card-6-desc": "We stay with you after delivery to ensure the best performance of your campaigns.",
    "why-stat-experience": "Years of Experience",
    "why-stat-designs": "Innovative Designs",
    "why-stat-print": "Delivered Prints",
    "why-stat-retention": "Client Satisfaction Rate",

    // Portfolio
    "pf-badge": "Our Portfolio",
    "pf-title": "Work that speaks for us",
    "pf-sub": "Samples of our projects in design, printing, brand identity, and ad campaigns.",
    "pf-btn-all": "All",
    "pf-btn-design": "Graphic Design",
    "pf-btn-print": "Printed Designs",
    "pf-btn-social": "Social Media",
    "pf-btn-ads": "Ad Campaigns",
    "full-zoom-hint": "🔍 Click image to Zoom & Pan",
    "pf-btn-videos": "Videos & Reels",

    // Process
    "proc-badge": "Work Steps",
    "proc-title": "How we work? Your project's journey with us",
    "proc-sub": "A streamlined, transparent process designed to maximize value and deliver results that make every minute and every investment worthwhile.",
    // Testimonials
    "testi-badge": "Client Feedback",
    "testi-title": "Our clients' trust is our greatest achievement",
    "testi-quote-1": "\"Forsa Media completely changed my platforms. From visual identity to Reels editing, fast communication, and organic ads results are very good.\"",
    "testi-avatar-1": "O",
    "testi-name-1": "Mr. Omar Azazy",
    "testi-role-1": "Financial / Market Analyst",
    "testi-quote-2": "\"Thank you very much. I will set up the page, and God willing, we have more work coming in animation and editing.. Delighted to work with you, and look forward to continuing together.\"",
    "testi-avatar-2": "M",
    "testi-name-2": "Mr. Mohamed Hafez",
    "testi-role-2": "Financial Markets Data Analyst",
    "testi-quote-3": "\"We worked with Forsa Media on printing and preparing company documents and promotional materials multiple times. Honestly, the commitment to deadlines, material quality, and colors was excellent and reflected true professionalism.\"",
    "testi-avatar-3": "A",
    "testi-name-3": "Arabia Online (NI Capital)",
    "testi-role-3": "Financial Brokerage Company",
    "testi-quote-4": "\"We pray to Allah to bless the efforts of Forsa Media. They provided outstanding designs for our digital platforms and a masterful editing of the academy podcast, combining visual sophistication and auditory clarity.\"",
    "testi-avatar-4": "W",
    "testi-name-4": "Wahy Academy",
    "testi-role-4": "Islamic Educational Platform",
    "testi-quote-5": "\"Great service and fast response from the team. They helped us a lot in producing charity posts and social media campaigns in a humane and engaging way that touches the hearts of donors and beneficiaries.\"",
    "testi-avatar-5": "U",
    "testi-name-5": "Ummat Al-Habeeb Charity",
    "testi-role-5": "Social Aid Association",
    "testi-quote-6": "\"The social media designs you made for our company contributed significantly to clarifying the features of our security systems and presenting them in a professional and convincing way to clients. Excellent work and refined communication.\"",
    "testi-avatar-6": "S",
    "testi-name-6": "Smartivia Company",
    "testi-role-6": "Security Systems & Cameras",
    "testi-quote-7": "\"Working on print materials with the Forsa Media team was an excellent experience. We requested prints in various shapes and materials, and the execution was highly accurate and fast with professional color matching.\"",
    "testi-avatar-7": "N",
    "testi-name-7": "Ms. Nihad Ali",
    "testi-role-7": "Manager of Al-Borsageya for Financial Solutions",
    "testi-quote-8": "\"They designed a comprehensive brand identity for our company and executed all official and promotional print materials multiple times. Their work is outstanding; they understand the market and add a strong professional touch to the identity.\"",
    "testi-avatar-8": "E",
    "testi-name-8": "EgyChem Company",
    "testi-role-8": "Plastic Raw Materials Trading",
    "testi-quote-9": "\"Fast communication, perfect execution, and fully matching the specifications we requested for our company print materials. Thank you to the Forsa Media team for their commitment to accuracy and high quality.\"",
    "testi-avatar-9": "T",
    "testi-name-9": "Mr. Mohamed",
    "testi-role-9": "Manager of Taher Steel Company",
    "testi-quote-10": "\"Dealing with you saved us a lot of effort, from packaging and menu design to final printing with excellent quality and amazing delivery speed. God willing, our collaboration will continue.\"",
    "testi-avatar-10": "K",
    "testi-name-10": "Mr. Karim",
    "testi-role-10": "Manager of General Supplies Company",
    "testi-quote-11": "\"The shape and design of the packaging (boxes and bags) made a huge difference with our restaurant customers. The printing quality and colors were honorable and attractive. Thank you very much for this wonderful effort.\"",
    "testi-avatar-11": "M",
    "testi-name-11": "Mr. Mohamed",
    "testi-role-11": "Manager of 999 Restaurant",
    "testi-prev": "Previous",
    "testi-next": "Next",

    // Packages
    "pkg-badge": "Our Offers",
    "pkg-title": "A package that matches your ambition",
    "pkg-sub": "Choose the right package for your business, or contact us to design a custom one.",
    "pkg-basic": "Basic Package",
    "pkg-advanced": "Advanced Package",
    "pkg-pro": "Professional Package",
    "pkg-price-period": "EGP / month",
    "pkg-btn": "Get Started",
    "pkg-badge-popular": "Most Popular",
    "pkg-basic-desc": "Suitable for startups beginning their digital presence.",
    "pkg-feat-basic-1": "12 professional social media designs.",
    "pkg-feat-basic-2": "1 platform management.",
    "pkg-feat-basic-3": "Caption writing.",
    "pkg-feat-basic-4": "Content publishing.",
    "pkg-feat-basic-5": "4 Stories design.",
    "pkg-feat-basic-6": "1 sponsored ad management (budget not included).",
    "pkg-feat-basic-7": "Monthly performance report.",
    "pkg-feat-basic-8": "WhatsApp support.",
    "pkg-adv-desc": "For businesses wanting steady growth and strong presence.",
    "pkg-feat-adv-1": "20 social media designs.",
    "pkg-feat-adv-2": "Management of up to 3 platforms.",
    "pkg-feat-adv-3": "Content & caption writing.",
    "pkg-feat-adv-4": "8 Stories design.",
    "pkg-feat-adv-5": "2 ad campaigns management.",
    "pkg-feat-adv-6": "Messages & comments moderation.",
    "pkg-feat-adv-7": "Weekly performance report.",
    "pkg-feat-adv-8": "Visual identity enhancement for content.",
    "pkg-feat-adv-9": "Priority execution.",
    "pkg-pro-desc": "Integrated solutions for large businesses and scaling brands.",
    "pkg-feat-pro-1": "30 social media designs (Fair-use policy applies).",
    "pkg-feat-pro-2": "All social media platforms management.",
    "pkg-feat-pro-3": "Full content writing.",
    "pkg-feat-pro-4": "Stories & Reels Covers design.",
    "pkg-feat-pro-5": "Continuous ad campaigns management.",
    "pkg-feat-pro-6": "Competitor analysis.",
    "pkg-feat-pro-7": "Monthly content plan.",
    "pkg-feat-pro-8": "Dedicated account manager.",
    "pkg-feat-pro-9": "Monthly follow-up meeting.",
    "pkg-feat-pro-10": "Discounts on design & printing services.",

    // Additional Services
    "extra-title": "Additional Services (On-Demand)",
    "extra-sub": "You can request any of these services independently to develop your project without committing to a full package.",
    "extra-item-1-title": "Brand Identity Design",
    "extra-item-1-desc": "Logo, colors, fonts, and complete applications that give your brand a distinctive personality.",
    "extra-item-1-price": "Starts from 1,500 EGP",
    "extra-item-2-title": "Reels Video Editing",
    "extra-item-2-desc": "Professional Reels and TikTok video editing including effects, music, and color grading.",
    "extra-item-2-price": "250 EGP / video",
    "extra-item-3-title": "Extra Platform Management",
    "extra-item-3-desc": "Manage an additional platform for the same business activity to expand your reach.",
    "extra-item-3-price": "500 EGP / month",
    "extra-item-4-title": "Extra Sponsored Campaign",
    "extra-item-4-desc": "Create and manage an additional targeted sponsored campaign to increase sales.",
    "extra-item-4-price": "400 EGP / month",
    "extra-item-5-title": "Content Writing & Photo Editing",
    "extra-item-5-desc": "Write custom post captions and edit images/posters compatible with your brand.",
    "extra-item-5-price": "600 EGP / month",
    "extra-item-6-title": "Single Logo Design",
    "extra-item-6-desc": "Design a professional logo for your brand to serve as the foundation of your visual identity.",
    "extra-item-6-price": "750 EGP",

    // FAQ
    "faq-badge": "Frequently Asked Questions",
    "faq-title": "Have a question? We have answers",
    "faq-q-1": "How long does design implementation take?",
    "faq-a-1": "The duration varies depending on the project type, but simple designs are delivered within 2-3 business days, while a full brand identity may take one to two weeks.",
    "faq-q-2": "Do you offer sponsored advertising services for all platforms?",
    "faq-a-2": "Yes, we offer well-planned sponsored campaigns on Facebook, Instagram, and TikTok to achieve maximum reach and best return.",
    "faq-q-3": "Do your services include printing and physical delivery?",
    "faq-a-3": "We cover all your printing needs from design through implementation and delivery to your door.",
    "faq-q-4": "How is the project price determined?",
    "faq-a-4": "The price depends on the type of service, project size, and duration. We offer fixed monthly packages or custom quotes for one-off projects.",
    "faq-q-5": "Can the design be modified after delivery?",
    "faq-a-5": "Yes, all our packages include up to 3 free revisions to ensure the best results, and any additional revisions will be at a low extra cost.",
    "faq-q-6": "How do you guarantee color and print quality?",
    "faq-a-6": "We fully review every design file technically before sending it to press, verifying color modes (CMYK), dimensions, and bleed margins to avoid any printing errors.",

    // Contact
    "contact-badge": "Contact Us",
    "contact-title": "We love to hear from you",
    "contact-sub": "Fill in the form, and we will get back to you as soon as possible. Or reach us through the channels below 👇.",
    "field-name": "Name",
    "field-phone": "Phone Number",
    "field-email": "Email Address",
    "field-biz": "Type of Business",
    "field-details": "Request Details",
    "placeholder-name": "e.g., John Doe",
    "placeholder-msg": "Tell us more about your project and needs...",
    "opt-restaurant": "Restaurant or Cafe",
    "opt-clinic": "Clinic or Center",
    "opt-education": "Educational Institution",
    "opt-realestate": "Real Estate Company",
    "opt-ecommerce": "E-commerce Store",
    "opt-company": "Company / Institution",
    "opt-other": "Other",
    "form-btn-send": "Send Inquiry via WhatsApp",
    "form-note": "Our team will contact you within 24 working hours.",
    "card-phone-title": "Call Us",
    "card-email-title": "Email Us",
    "card-map-title": "Our Location",
    "card-map-desc": "Al Moatamedeya St, after Electric Ring Road exit - Ard El Lewa - Giza - Egypt",
    "social-whatsapp": "WhatsApp",
    "social-facebook": "Facebook",
    "social-instagram": "Instagram",
    "map-pin-text": "Giza - Egypt",

    // Footer
    "footer-brand-desc": "Forsa Media: Specializing in Graphic Design, Printing, Social Media Management, and Sponsored Ads. Towards a chance.. that makes a difference.",
    "footer-links-title": "Quick Links",
    "footer-contact-title": "Contact Us",
    "footer-contact-desc": "Giza - Egypt",
    "footer-copy": "© 2026 Forsa Media. All rights reserved.",
    "footer-by": "💚 Designed by Forsa Media Team",
    "footer-services-title": "Services",
    "footer-service-identity": "Visual Identity",
    "footer-service-print": "Advertising & Printing",
    "footer-service-social": "Social Media Management",
    "footer-service-ads": "Sponsored Ads",
    "nav-toggle-aria": "Toggle Menu",
    "hero-chip-1": "Creative Design",
    "hero-chip-2": "Quality Printing",
    "hero-chip-3": "Fast Delivery",
    "hero-chip-4": "Page Management",
    "hero-chip-5": "Video Editing",
    "hero-chip-6": "Paid Ads",
    "hero-chip-7": "Packaging Design",
    "hero-scroll": "Scroll Down",
    "proc-step-1-title": "Client Understanding",
    "proc-step-1-desc": "We listen to your goals and understand your project nature.",
    "proc-step-2-title": "Strategy",
    "proc-step-2-desc": "We create a studied marketing plan tailored to your market.",
    "proc-step-3-title": "Design",
    "proc-step-3-desc": "We transform the strategy into tangible, stunning designs.",
    "proc-step-4-title": "Review",
    "proc-step-4-desc": "We review the details with you until you are fully satisfied.",
    "proc-step-5-title": "Launch",
    "proc-step-5-desc": "We publish and execute the campaign or project completely.",
    "proc-step-6-title": "Optimization",
    "proc-step-6-desc": "We monitor performance and continuously optimize results.",
  }
};

(function(){
  "use strict";

  /* ============================================
     THEME TOGGLE (LIGHT / DARK)
     ============================================ */
  const themeToggle = document.getElementById('themeToggle');
  const currentTheme = localStorage.getItem('theme') || 'light';

  document.documentElement.setAttribute('data-theme', currentTheme);
  updateThemeIcon(currentTheme);

  themeToggle.addEventListener('click', () => {
    const activeTheme = document.documentElement.getAttribute('data-theme');
    const newTheme = activeTheme === 'dark' ? 'light' : 'dark';
    
    document.documentElement.setAttribute('data-theme', newTheme);
    localStorage.setItem('theme', newTheme);
    updateThemeIcon(newTheme);
  });

  function updateThemeIcon(theme) {
    const sunIcon = document.getElementById('theme-sun');
    const moonIcon = document.getElementById('theme-moon');
    if (theme === 'dark') {
      sunIcon.style.display = 'block';
      moonIcon.style.display = 'none';
    } else {
      sunIcon.style.display = 'none';
      moonIcon.style.display = 'block';
    }
  }

  /* ============================================
     MULTILINGUAL TRANSLATION SYSTEM
     ============================================ */
  const langToggle = document.getElementById('langToggle');
  const currentLang = localStorage.getItem('lang') || 'ar';

  langToggle.addEventListener('click', () => {
    const activeLang = document.documentElement.getAttribute('dir') === 'rtl' ? 'en' : 'ar';
    setLanguage(activeLang);
  });

  function setLanguage(lang) {
    localStorage.setItem('lang', lang);
    if (lang === 'en') {
      document.documentElement.setAttribute('dir', 'ltr');
      document.documentElement.setAttribute('lang', 'en');
      document.body.setAttribute('dir', 'ltr');
      document.body.setAttribute('lang', 'en');
      langToggle.textContent = 'AR';
    } else {
      document.documentElement.setAttribute('dir', 'rtl');
      document.documentElement.setAttribute('lang', 'ar');
      document.body.setAttribute('dir', 'rtl');
      document.body.setAttribute('lang', 'ar');
      langToggle.textContent = 'EN';
    }
    
    document.querySelectorAll('[data-i18n]').forEach(el => {
      const key = el.dataset.i18n;
      if (translations[lang] && translations[lang][key]) {
        if (el.tagName === 'META') {
          el.setAttribute('content', translations[lang][key]);
        } else {
          el.textContent = translations[lang][key];
        }
      }
    });

    document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
      const key = el.dataset.i18nPlaceholder;
      if (translations[lang] && translations[lang][key]) {
        el.setAttribute('placeholder', translations[lang][key]);
      }
    });

    document.querySelectorAll('[data-i18n-aria-label]').forEach(el => {
      const key = el.dataset.i18nAriaLabel;
      if (translations[lang] && translations[lang][key]) {
        el.setAttribute('aria-label', translations[lang][key]);
      }
    });
    renderPortfolio(lang);
  }

  /* ============================================
     HEADER SCROLL STATE
     ============================================ */
  const header = document.getElementById('siteHeader');
  const onScroll = () => {
    header.classList.toggle('scrolled', window.scrollY > 30);
  };
  document.addEventListener('scroll', onScroll, {passive:true});
  onScroll();

  /* ============================================
     MOBILE NAV TOGGLE
     ============================================ */
  const navToggle = document.getElementById('navToggle');
  const navLinks = document.getElementById('navLinks');
  navToggle.addEventListener('click', () => {
    const open = navLinks.classList.toggle('open');
    navToggle.classList.toggle('open', open);
    navToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
  });
  navLinks.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => {
      navLinks.classList.remove('open');
      navToggle.classList.remove('open');
      navToggle.setAttribute('aria-expanded', 'false');
    });
  });

  /* ============================================
     SCROLL REVEAL (INTERSECTION OBSERVER)
     ============================================ */
  const revealEls = document.querySelectorAll('.reveal');
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting){
        entry.target.classList.add('in-view');
        revealObserver.unobserve(entry.target);
      }
    });
  }, {threshold:0.12});
  revealEls.forEach(el => revealObserver.observe(el));

  /* ============================================
     ANIMATED STAT COUNTERS
     ============================================ */
  const counters = document.querySelectorAll('[data-count]');
  const countObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      const el = entry.target;
      const target = parseFloat(el.dataset.count);
      const suffix = el.dataset.suffix || '+';
      const duration = 1500;
      const start = performance.now();
      function tick(now){
        const progress = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        const value = (target * eased);
        el.textContent = (target % 1 === 0 ? Math.floor(value) : value.toFixed(1)) + suffix;
        if (progress < 1) requestAnimationFrame(tick);
        else el.textContent = (target % 1 === 0 ? target : target.toFixed(1)) + suffix;
      }
      requestAnimationFrame(tick);
      countObserver.unobserve(el);
    });
  }, {threshold:0.4});
  counters.forEach(el => countObserver.observe(el));

  /* ============================================
     HERO BACKGROUND PARTICLES
     ============================================ */
  const particleHost = document.getElementById('heroParticles');
  if (particleHost){
    const count = window.innerWidth < 640 ? 8 : 16;
    for (let i = 0; i < count; i++){
      const p = document.createElement('span');
      p.className = 'hero-particle';
      const size = 3 + Math.random() * 5;
      p.style.width = size + 'px';
      p.style.height = size + 'px';
      p.style.right = Math.random() * 100 + '%';
      p.style.bottom = Math.random() * 60 + '%';
      p.style.animationDuration = (6 + Math.random() * 6) + 's';
      p.style.animationDelay = (Math.random() * 6) + 's';
      particleHost.appendChild(p);
    }
  }

  /* ============================================
     PORTFOLIO DATA & DYNAMIC RENDER
     ============================================ */
  // Lightbox Modal Functionality
  const lightbox = document.getElementById('portfolioLightbox');
  const lightboxClose = document.getElementById('lightboxClose');
  const mediaContainer = document.getElementById('lightboxMediaContainer');
  const lightboxCat = document.getElementById('lightboxCat');
  const lightboxTitle = document.getElementById('lightboxTitle');

  if (lightboxClose && lightbox) {
    lightboxClose.addEventListener('click', () => {
      lightbox.classList.remove('active');
      mediaContainer.classList.remove('has-carousel');
      mediaContainer.innerHTML = '';
    });

    // Close when clicking outside content
    lightbox.addEventListener('click', (e) => {
      if (e.target === lightbox) {
        lightbox.classList.remove('active');
        mediaContainer.classList.remove('has-carousel');
        mediaContainer.innerHTML = '';
      }
    });
  }

  let activeMedia = [];
  let currentSlide = 0;

  function d(b) {
    if (!b) return '';
    try { return decodeURIComponent(escape(atob(b))); } catch(e) { return b; }
  }

  function safeMediaSrc(path) {
    if (!path) return '';
    const decoded = d(path);
    return decoded.replace(/\?/g, '%3F').replace(/#/g, '%23').replace(/'/g, '%27');
  }

  function initLightboxZoom(imgEl) {
    if (!imgEl) return;
    if (imgEl._zoomInitialized) return;
    imgEl._zoomInitialized = true;
    imgEl._isZoomed = false;

    imgEl.addEventListener('click', (e) => {
      e.stopPropagation();
      imgEl._isZoomed = !imgEl._isZoomed;
      if (imgEl._isZoomed) {
        imgEl.classList.add('zoomed');
        imgEl.style.transform = 'scale(2.5)';
        updateZoomOrigin(e, imgEl);
      } else {
        resetZoom(imgEl);
      }
    });

    imgEl.addEventListener('mousemove', (e) => {
      if (imgEl._isZoomed) {
        updateZoomOrigin(e, imgEl);
      }
    });

    imgEl.addEventListener('touchmove', (e) => {
      if (imgEl._isZoomed && e.touches.length > 0) {
        updateZoomOrigin(e.touches[0], imgEl);
      }
    }, { passive: true });
  }

  function updateZoomOrigin(event, imgEl) {
    const rect = imgEl.getBoundingClientRect();
    const x = ((event.clientX - rect.left) / rect.width) * 100;
    const y = ((event.clientY - rect.top) / rect.height) * 100;
    const safeX = Math.max(0, Math.min(100, x));
    const safeY = Math.max(0, Math.min(100, y));
    imgEl.style.transformOrigin = `${safeX}% ${safeY}%`;
  }

  function resetZoom(imgEl) {
    if (imgEl) {
      imgEl._isZoomed = false;
      imgEl.classList.remove('zoomed');
      imgEl.style.transform = 'scale(1)';
      imgEl.style.transformOrigin = 'center center';
    }
  }

  function showSlide(index) {
    if (activeMedia.length === 0) return;
    
    mediaContainer.innerHTML = '';
    
    if (index >= activeMedia.length) index = 0;
    if (index < 0) index = activeMedia.length - 1;
    currentSlide = index;

    const rawSrc = activeMedia[currentSlide];
    const src = typeof rawSrc === 'object' ? (rawSrc.cover || rawSrc.video) : rawSrc;
    const isVideo = src.endsWith('.mp4') || src.endsWith('.webm') || src.endsWith('.mov');

    if (isVideo) {
      const videoEl = document.createElement('video');
      videoEl.src = safeMediaSrc(src);
      videoEl.controls = true;
      videoEl.autoplay = true;
      videoEl.setAttribute('controlsList', 'nodownload');
      videoEl.addEventListener('contextmenu', e => e.preventDefault());
      videoEl.className = 'lightbox-video';
      mediaContainer.appendChild(videoEl);
    } else {
      const imgEl = document.createElement('img');
      imgEl.src = safeMediaSrc(src);
      imgEl.addEventListener('contextmenu', e => e.preventDefault());
      imgEl.addEventListener('dragstart', e => e.preventDefault());
      imgEl.className = 'lightbox-img';
      mediaContainer.appendChild(imgEl);
      initLightboxZoom(imgEl);
    }

    // Update dots
    const dots = lightbox.querySelectorAll('.lightbox-dot');
    dots.forEach((dot, idx) => {
      dot.classList.toggle('active', idx === currentSlide);
    });

    // Update thumbnails
    const thumbs = lightbox.querySelectorAll('.lightbox-thumb');
    thumbs.forEach((thumb, idx) => {
      const isActive = idx === currentSlide;
      thumb.classList.toggle('active', isActive);
      if (isActive) {
        thumb.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
      }
    });
  }

  const fullZoomModal = document.getElementById('fullImageZoomModal');
  const fullZoomClose = document.getElementById('fullZoomClose');
  const fullZoomImg = document.getElementById('fullZoomImg');

  if (fullZoomModal && fullZoomClose) {
    fullZoomClose.addEventListener('click', () => {
      fullZoomModal.classList.remove('active');
      if (fullZoomImg) resetZoom(fullZoomImg);
    });

    fullZoomModal.addEventListener('click', (e) => {
      if (e.target === fullZoomModal) {
        fullZoomModal.classList.remove('active');
        if (fullZoomImg) resetZoom(fullZoomImg);
      }
    });
  }

  function openFullImageZoomModal(src, title) {
    if (!fullZoomModal || !fullZoomImg) return;
    fullZoomImg.src = src;
    fullZoomImg.alt = title || '';
    resetZoom(fullZoomImg);
    initLightboxZoom(fullZoomImg);
    fullZoomModal.classList.add('active');
  }

  function openFullVideoModal(videoSrc, title) {
    const videoModal = document.createElement('div');
    videoModal.className = 'full-zoom-modal active';
    videoModal.style.zIndex = '25000';

    const closeBtn = document.createElement('button');
    closeBtn.className = 'full-zoom-close';
    closeBtn.textContent = '\u00D7';
    closeBtn.addEventListener('click', () => videoModal.remove());

    const body = document.createElement('div');
    body.className = 'full-zoom-body';
    body.style.cssText = 'background:#000; padding:10px;';

    const videoEl = document.createElement('video');
    videoEl.src = videoSrc;
    videoEl.controls = true;
    videoEl.autoplay = true;
    videoEl.setAttribute('playsinline', '');
    videoEl.setAttribute('controlsList', 'nodownload');
    videoEl.addEventListener('contextmenu', e => e.preventDefault());
    videoEl.style.cssText = 'max-width:100%; max-height:100%; border-radius:12px; box-shadow: 0 10px 40px rgba(0,0,0,0.8);';

    body.appendChild(videoEl);
    videoModal.appendChild(closeBtn);
    videoModal.appendChild(body);
    videoModal.addEventListener('click', (e) => {
      if (e.target === videoModal) videoModal.remove();
    });
    document.body.appendChild(videoModal);
  }

  function init3DCarousel(container, wrapper, mediaList, categoryLabel) {
    function getCleanTitle(itemSrc) {
      const src = (typeof itemSrc === 'object' && itemSrc !== null) ? (itemSrc.video || itemSrc.cover) : itemSrc;
      const filename = String(src).split('/').pop().replace(/\.[^/.]+$/, "");
      return decodeURIComponent(filename);
    }

    container.innerHTML = mediaList.map((item, i) => {
      const isObj = typeof item === 'object' && item !== null;
      const coverSrcRaw = isObj ? item.cover : item;
      const videoSrcRaw = isObj ? item.video : '';
      const coverSrc = d(coverSrcRaw);
      const videoSrc = d(videoSrcRaw);
      const itemDecoded = isObj ? { cover: coverSrc, video: videoSrc } : coverSrc;
      const rawVideo = isObj ? videoSrc : (typeof coverSrc === 'string' && (coverSrc.endsWith('.mp4') || coverSrc.endsWith('.webm') || coverSrc.endsWith('.mov')) ? coverSrc : '');
      const cleanTitle = getCleanTitle(itemDecoded);
      const isVideo = !!rawVideo;
      const isStandaloneVideo = isVideo && !isObj;

      function enc(p) { return p ? p.replace(/\?/g, '%3F').replace(/#/g, '%23').replace(/'/g, '%27') : ''; }
      const safeCover = enc(coverSrc);
      const safeVideo = enc(rawVideo);

      // Escape attribute values to prevent HTML injection
      function escAttr(s) {
        return String(s).replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/'/g,'&#39;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
      }
      const safeTitle = escAttr(cleanTitle);

      let mediaHtml;
      if (isStandaloneVideo) {
        mediaHtml = `<video class="carousel-thumb-video" src="${safeVideo}#t=0.001" muted playsinline preload="metadata" style="width:100%; height:100%; object-fit:cover; border-radius:14px; display:block;" data-thumb="1"></video>`;
      } else {
        mediaHtml = `<img src="${safeCover}" alt="${safeTitle}" loading="lazy" decoding="async" />`;
      }

      return `
        <div class="portfolio-card ${isVideo ? 'video-card' : ''}" data-idx="${i}" data-src="${safeCover}" data-video="${safeVideo}" data-title="${safeTitle}" style="--idx: ${i}">
          ${mediaHtml}
          ${isVideo ? `
            <div class="video-play-indicator" style="z-index: 4;">
              <svg class="icon-svg" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"></polygon></svg>
            </div>
          ` : ''}
        </div>
      `;
    }).join('');

    // After building HTML, seek all thumb videos to first frame
    requestAnimationFrame(() => {
      container.querySelectorAll('video[data-thumb]').forEach(v => {
        const seek = () => { try { v.currentTime = 0.1; } catch(e) {} };
        if (v.readyState >= 1) { seek(); }
        else { v.addEventListener('loadedmetadata', seek, { once: true }); }
      });
    });

    const cards = Array.from(container.querySelectorAll('.portfolio-card'));

    function updateCenterCard() {
      const wrapperRect = wrapper.getBoundingClientRect();
      const wrapperCenter = wrapperRect.left + wrapperRect.width / 2;

      let closestCard = null;
      let minDistance = Infinity;

      cards.forEach(card => {
        const cardRect = card.getBoundingClientRect();
        const cardCenter = cardRect.left + cardRect.width / 2;
        const dist = Math.abs(wrapperCenter - cardCenter);

        if (dist < minDistance) {
          minDistance = dist;
          closestCard = card;
        }
      });

      cards.forEach(card => {
        const cardRect = card.getBoundingClientRect();
        const cardCenter = cardRect.left + cardRect.width / 2;

        card.classList.remove('center-card', 'left-card', 'right-card');

        if (card === closestCard) {
          card.classList.add('center-card');
        } else if (cardCenter < wrapperCenter) {
          card.classList.add('left-card');
        } else {
          card.classList.add('right-card');
        }
      });

      // Update caption below
      if (closestCard) {
        const idx = parseInt(closestCard.dataset.idx);
        const title = closestCard.dataset.title;
        const total = mediaList.length;

        const formattedIndex = String(idx + 1).padStart(2, '0');
        const formattedTotal = String(total).padStart(2, '0');

        if (lightboxCat) {
          lightboxCat.innerHTML = '';

          const catLabelSpan = document.createElement('span');
          catLabelSpan.className = 'cat-label';
          catLabelSpan.textContent = categoryLabel || '';

          const counterBadge = document.createElement('span');
          counterBadge.className = 'counter-badge';

          const counterCurrSpan = document.createElement('span');
          counterCurrSpan.className = 'counter-curr';
          counterCurrSpan.id = 'counterCurr';
          counterCurrSpan.textContent = formattedIndex;

          const slashSpan = document.createElement('span');
          slashSpan.className = 'counter-slash';
          slashSpan.textContent = '/';

          const totalSpan = document.createElement('span');
          totalSpan.className = 'counter-total';
          totalSpan.textContent = formattedTotal;

          counterBadge.appendChild(counterCurrSpan);
          counterBadge.appendChild(slashSpan);
          counterBadge.appendChild(totalSpan);
          lightboxCat.appendChild(catLabelSpan);
          lightboxCat.appendChild(counterBadge);

          const counterCurr = document.getElementById('counterCurr');
          if (counterCurr) {
            counterCurr.classList.remove('num-pop');
            void counterCurr.offsetWidth;
            counterCurr.classList.add('num-pop');
          }
        }
        if (lightboxTitle) {
          lightboxTitle.textContent = title;
        }
      }
    }

    wrapper.addEventListener('scroll', updateCenterCard, { passive: true });
    window.addEventListener('resize', updateCenterCard, { passive: true });

    setTimeout(() => {
      if (cards[0]) {
        const cardRect = cards[0].getBoundingClientRect();
        const wrapperRect = wrapper.getBoundingClientRect();
        wrapper.scrollLeft = cards[0].offsetLeft - (wrapperRect.width / 2) + (cardRect.width / 2);
      }
      updateCenterCard();
    }, 100);

    let isDown = false;
    let hasDragged = false;
    let startX, scrollLeft;

    wrapper.addEventListener('mousedown', (e) => {
      isDown = true;
      hasDragged = false;
      startX = e.pageX - wrapper.offsetLeft;
      scrollLeft = wrapper.scrollLeft;
    });
    wrapper.addEventListener('mouseleave', () => { isDown = false; });
    wrapper.addEventListener('mouseup', () => { isDown = false; });
    wrapper.addEventListener('mousemove', (e) => {
      if(!isDown) return;
      const x = e.pageX - wrapper.offsetLeft;
      if (Math.abs(x - startX) > 6) {
        hasDragged = true;
      }
      e.preventDefault();
      const walk = (x - startX) * 1.5;
      wrapper.scrollLeft = scrollLeft - walk;
    });

    wrapper.addEventListener('touchstart', (e) => {
      isDown = true;
      hasDragged = false;
      startX = e.touches[0].pageX - wrapper.offsetLeft;
      scrollLeft = wrapper.scrollLeft;
    }, { passive: true });
    wrapper.addEventListener('touchend', () => { isDown = false; });
    wrapper.addEventListener('touchmove', (e) => {
      if(!isDown) return;
      const x = e.touches[0].pageX - wrapper.offsetLeft;
      if (Math.abs(x - startX) > 6) {
        hasDragged = true;
      }
      const walk = (x - startX) * 1.5;
      wrapper.scrollLeft = scrollLeft - walk;
    }, { passive: true });

    cards.forEach(card => {
      card.addEventListener('click', (e) => {
        e.stopPropagation();
        if (hasDragged) {
          hasDragged = false;
          return;
        }
        if (card.classList.contains('center-card')) {
          const videoSrc = card.dataset.video;
          if (videoSrc && videoSrc.length > 0) {
            openFullVideoModal(videoSrc, card.dataset.title);
          } else {
            openFullImageZoomModal(card.dataset.src, card.dataset.title);
          }
        } else {
          const cardRect = card.getBoundingClientRect();
          const wrapperRect = wrapper.getBoundingClientRect();
          const delta = (cardRect.left + cardRect.width / 2) - (wrapperRect.left + wrapperRect.width / 2);
          wrapper.scrollBy({ left: delta, behavior: 'smooth' });
        }
      });
    });

    return {
      next: () => {
        const centerCard = container.querySelector('.portfolio-card.center-card');
        const targetCard = centerCard ? centerCard.nextElementSibling : cards[0];
        if (targetCard) {
          const cardRect = targetCard.getBoundingClientRect();
          const wrapperRect = wrapper.getBoundingClientRect();
          const delta = (cardRect.left + cardRect.width / 2) - (wrapperRect.left + wrapperRect.width / 2);
          wrapper.scrollBy({ left: delta, behavior: 'smooth' });
        }
      },
      prev: () => {
        const centerCard = container.querySelector('.portfolio-card.center-card');
        const targetCard = centerCard ? centerCard.previousElementSibling : cards[0];
        if (targetCard) {
          const cardRect = targetCard.getBoundingClientRect();
          const wrapperRect = wrapper.getBoundingClientRect();
          const delta = (cardRect.left + cardRect.width / 2) - (wrapperRect.left + wrapperRect.width / 2);
          wrapper.scrollBy({ left: delta, behavior: 'smooth' });
        }
      }
    };
  }

  function openLightbox(item, lang) {
    if (!lightbox || !mediaContainer) return;
    mediaContainer.innerHTML = '';
    mediaContainer.classList.remove('has-carousel');
    
    const content = item[lang] || item['ar'];
    lightboxCat.textContent = content.cat;
    lightboxTitle.textContent = content.title;

    // Meta Pixel Tracking: Portfolio View (ViewContent) & Project Details (ProjectViewed)
    if (window.ForsaPixel) {
      window.ForsaPixel.trackPortfolioView(item, content);
    }

    activeMedia = [];
    if (item.media && Array.isArray(item.media) && item.media.length > 0) {
      activeMedia = item.media;
    } else {
      if (item.video) activeMedia.push(item.video);
      if (item.image) activeMedia.push(item.image);
    }

    const existingArrows = lightbox.querySelectorAll('.lightbox-arrow');
    existingArrows.forEach(el => el.remove());
    const existingDots = lightbox.querySelectorAll('.lightbox-dots');
    existingDots.forEach(el => el.remove());
    const existingThumbs = lightbox.querySelectorAll('.lightbox-thumbnails');
    existingThumbs.forEach(el => el.remove());

    const chevronLeft = `<svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"><polyline points="15 18 9 12 15 6"></polyline></svg>`;
    const chevronRight = `<svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"><polyline points="9 18 15 12 9 6"></polyline></svg>`;

    if (activeMedia.length > 1) {
      mediaContainer.classList.add('has-carousel');
      mediaContainer.innerHTML = `
        <div class="portfolio-carousel-wrapper" id="smCarouselWrapper">
          <div class="portfolio-carousel" id="smCarousel"></div>
        </div>
      `;
      const smCarousel = document.getElementById('smCarousel');
      const smCarouselWrapper = document.getElementById('smCarouselWrapper');
      const carouselController = init3DCarousel(smCarousel, smCarouselWrapper, activeMedia, content.cat);

      const contentEl = lightbox.querySelector('.lightbox-content');
      const isRtl = document.documentElement.getAttribute('dir') === 'rtl' || lang === 'ar';

      const prevBtn = document.createElement('button');
      prevBtn.className = 'lightbox-arrow prev';
      prevBtn.innerHTML = chevronLeft;
      prevBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        if (isRtl) {
          carouselController.next();
        } else {
          carouselController.prev();
        }
      });
      contentEl.appendChild(prevBtn);

      const nextBtn = document.createElement('button');
      nextBtn.className = 'lightbox-arrow next';
      nextBtn.innerHTML = chevronRight;
      nextBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        if (isRtl) {
          carouselController.prev();
        } else {
          carouselController.next();
        }
      });
      contentEl.appendChild(nextBtn);
    } else {
      if (activeMedia.length > 1) {
        const contentEl = lightbox.querySelector('.lightbox-content');
        const isRtl = document.documentElement.getAttribute('dir') === 'rtl' || lang === 'ar';
        
        const prevBtn = document.createElement('button');
        prevBtn.className = 'lightbox-arrow prev';
        prevBtn.innerHTML = chevronLeft;
        prevBtn.addEventListener('click', (e) => {
          e.stopPropagation();
          showSlide(isRtl ? currentSlide + 1 : currentSlide - 1);
        });
        contentEl.appendChild(prevBtn);

        const nextBtn = document.createElement('button');
        nextBtn.className = 'lightbox-arrow next';
        nextBtn.innerHTML = chevronRight;
        nextBtn.addEventListener('click', (e) => {
          e.stopPropagation();
          showSlide(isRtl ? currentSlide - 1 : currentSlide + 1);
        });
        contentEl.appendChild(nextBtn);

        const dotsContainer = document.createElement('div');
        dotsContainer.className = 'lightbox-dots';
        activeMedia.forEach((_, idx) => {
          const dot = document.createElement('div');
          dot.className = 'lightbox-dot';
          dot.addEventListener('click', (e) => {
            e.stopPropagation();
            showSlide(idx);
          });
          dotsContainer.appendChild(dot);
        });
        contentEl.appendChild(dotsContainer);

        const thumbsContainer = document.createElement('div');
        thumbsContainer.className = 'lightbox-thumbnails';
        activeMedia.forEach((src, idx) => {
          const thumb = document.createElement('div');
          thumb.className = `lightbox-thumb ${idx === 0 ? 'active' : ''}`;
          const isVideo = src.endsWith('.mp4') || src.endsWith('.webm') || src.endsWith('.mov');
          if (isVideo) {
            const badge = document.createElement('span');
            badge.className = 'thumb-type-badge';
            badge.textContent = (lang === 'ar' ? '\u0641\u064A\u062F\u064A\u0648' : 'Video') + ' ' + (idx + 1);
            const vidEl = document.createElement('video');
            vidEl.src = src;
            vidEl.muted = true;
            vidEl.setAttribute('playsinline', '');
            vidEl.setAttribute('controlsList', 'nodownload');
            vidEl.addEventListener('contextmenu', e => e.preventDefault());
            vidEl.addEventListener('dragstart', e => e.preventDefault());
            thumb.appendChild(badge);
            thumb.appendChild(vidEl);
          } else {
            const imgEl = document.createElement('img');
            imgEl.src = src;
            imgEl.addEventListener('contextmenu', e => e.preventDefault());
            imgEl.addEventListener('dragstart', e => e.preventDefault());
            thumb.appendChild(imgEl);
          }
          thumb.addEventListener('click', (e) => {
            e.stopPropagation();
            showSlide(idx);
          });
          thumbsContainer.appendChild(thumb);
        });
        contentEl.appendChild(thumbsContainer);

        showSlide(0);
      } else if (activeMedia.length === 1) {
        showSlide(0);
      } else {
        const fallbackEl = document.createElement('div');
        fallbackEl.className = 'lightbox-fallback';
        fallbackEl.style.background = item.gradient || 'var(--card-bg)';
        const emojiSpan = document.createElement('span');
        emojiSpan.style.fontSize = '80px';
        emojiSpan.textContent = item.bg || '';
        fallbackEl.appendChild(emojiSpan);
        mediaContainer.appendChild(fallbackEl);
      }
    }

    lightbox.classList.add('active');
  }

  function renderPortfolio(lang) {
    const grid = document.getElementById('portfolioGrid');
    if (!grid) return;
    grid.innerHTML = '';
    
    // Get active filter
    const activeBtn = document.querySelector('.filter-btn.active');
    const activeFilter = activeBtn ? activeBtn.dataset.filter : 'all';
    
    // Check if portfolioData is loaded (from portfolio-data.js)
    const data = typeof portfolioData !== 'undefined' ? portfolioData : [];
    
    data.forEach((item, index) => {
      const itemEl = document.createElement('div');
      itemEl.className = 'portfolio-item reveal';
      itemEl.style.setProperty('--i', index);
      itemEl.dataset.cat = item.category;
      
      const match = activeFilter === 'all' || item.category === activeFilter;
      if (!match) {
        itemEl.classList.add('hide');
      }
      
      const content = item[lang] || item['ar'];
      
      // Determine background styles and video indicator from either single properties or the media list
      let hasImage = item.image;
      let hasVideo = item.video;
      let thumbnailSrc = d(item.image);

      if (item.media && Array.isArray(item.media) && item.media.length > 0) {
        const firstMedia = item.media[0];
        const isObj = typeof firstMedia === 'object' && firstMedia !== null;
        const mediaSrcRaw = isObj ? (firstMedia.cover || firstMedia.video || '') : String(firstMedia);
        const mediaSrc = d(mediaSrcRaw);
        const isFirstVideo = isObj ? (!!firstMedia.video && !firstMedia.cover) : (mediaSrc.endsWith('.mp4') || mediaSrc.endsWith('.webm') || mediaSrc.endsWith('.mov'));
        
        if (isFirstVideo) {
          hasVideo = true;
        } else {
          hasImage = true;
          if (!item.image) {
            thumbnailSrc = mediaSrc;
          }
        }

        const anyVideo = item.media.some(m => {
          if (typeof m === 'object' && m !== null) return !!m.video;
          if (typeof m === 'string') { const dec = d(m); return dec.endsWith('.mp4') || dec.endsWith('.webm') || dec.endsWith('.mov'); }
          return false;
        });
        if (anyVideo) {
          hasVideo = true;
        }
      }

      let firstVideoSrc = '';
      if (hasVideo) {
        const firstVideoSrcRaw = (item.media && Array.isArray(item.media))
          ? item.media.map(m => d((typeof m === 'object' && m !== null) ? m.video : m)).find(src => typeof src === 'string' && (src.endsWith('.mp4') || src.endsWith('.webm') || src.endsWith('.mov')))
          : d(item.video);
        firstVideoSrc = firstVideoSrcRaw || '';
      }

      // Build pf-bg div safely using DOM API
      const pfBg = document.createElement('div');
      pfBg.className = 'pf-bg';

      if (hasImage && thumbnailSrc) {
        const safeSrc = thumbnailSrc.replace(/\?/g, '%3F').replace(/#/g, '%23').replace(/'/g, '%27');
        pfBg.style.backgroundImage = `url('${safeSrc}')`;
      } else if (item.gradient) {
        pfBg.style.background = item.gradient;
      }

      if (hasVideo && firstVideoSrc && !hasImage) {
        const videoEl = document.createElement('video');
        videoEl.className = 'pf-video-preview';
        videoEl.src = firstVideoSrc + '#t=0.001';
        videoEl.muted = true;
        videoEl.setAttribute('playsinline', '');
        videoEl.setAttribute('preload', 'metadata');
        videoEl.setAttribute('controlsList', 'nodownload');
        videoEl.addEventListener('contextmenu', e => e.preventDefault());
        videoEl.addEventListener('dragstart', e => e.preventDefault());
        pfBg.appendChild(videoEl);
      } else if (!hasImage) {
        const emojiSpan = document.createElement('span');
        emojiSpan.textContent = item.bg || '';
        pfBg.appendChild(emojiSpan);
      }

      const indicatorText = lang === 'ar' ? 'استعرض الأعمال' : 'Explore Work';
      const indicator = document.createElement('div');
      indicator.className = 'video-play-indicator';
      indicator.style.zIndex = '2';

      // Static SVG icon — safe because it's a static string, not user data
      const iconSvg = hasVideo
        ? '<svg class="icon-svg" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>'
        : '<svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>';
      indicator.innerHTML = iconSvg; // static SVG only — safe

      const indicatorTextEl = document.createElement('span');
      indicatorTextEl.className = 'play-indicator-text';
      indicatorTextEl.textContent = indicatorText;
      indicator.appendChild(indicatorTextEl);
      pfBg.appendChild(indicator);
      itemEl.appendChild(pfBg);

      // Build pf-overlay div safely
      const pfOverlay = document.createElement('div');
      pfOverlay.className = 'pf-overlay';

      const catSpan = document.createElement('span');
      catSpan.className = 'pf-cat';
      catSpan.textContent = content.cat;
      pfOverlay.appendChild(catSpan);

      const titleH3 = document.createElement('h3');
      titleH3.className = 'pf-title';
      titleH3.textContent = content.title;
      pfOverlay.appendChild(titleH3);
      itemEl.appendChild(pfOverlay);

      // Open lightbox on click
      itemEl.addEventListener('click', () => {
        openLightbox(item, lang);
      });

      grid.appendChild(itemEl);
      if (typeof revealObserver !== 'undefined' && revealObserver) {
        revealObserver.observe(itemEl);
      }
    });
  }

  /* ============================================
     PORTFOLIO GRID FILTER
     ============================================ */
  const filterBtns = document.querySelectorAll('.filter-btn');
  filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      filterBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      const filter = btn.dataset.filter;
      if (window.ForsaPixel) {
        window.ForsaPixel.trackCategoryView(filter, btn.textContent.trim());
      }
      document.querySelectorAll('.portfolio-item').forEach(item => {
        const match = filter === 'all' || item.dataset.cat === filter;
        item.classList.toggle('hide', !match);
      });
    });
  });

  /* ============================================
     WORK PROCESS TIMELINE
     ============================================ */
  const timeline = document.getElementById('timeline');
  const timelineProgress = document.getElementById('timelineProgress');
  const steps = document.querySelectorAll('.step');
  if (timeline){
    const stepObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting){
          entry.target.classList.add('in-view');
          const idx = parseInt(entry.target.dataset.step, 10);
          const pct = ((idx + 1) / steps.length) * 92;
          if (timelineProgress) {
            if (window.innerWidth <= 768) {
              timelineProgress.style.height = pct + '%';
              timelineProgress.style.width = '2px';
            } else {
              timelineProgress.style.width = pct + '%';
              timelineProgress.style.height = '2px';
            }
          }
        }
      });
    }, {threshold:0.6});
    steps.forEach(s => stepObserver.observe(s));
  }

  /* ============================================
     TESTIMONIALS SLIDER
     ============================================ */
  const testiCards = document.querySelectorAll('.testi-card');
  const testiDots = document.querySelectorAll('.testi-dot');
  let testiIndex = 0;
  let testiTimer;
  function showTesti(i){
    testiIndex = (i + testiCards.length) % testiCards.length;
    testiCards.forEach((c, idx) => c.classList.toggle('active', idx === testiIndex));
    testiDots.forEach((d, idx) => d.classList.toggle('active', idx === testiIndex));
  }
  function nextTesti(){ showTesti(testiIndex + 1); }
  function prevTesti(){ showTesti(testiIndex - 1); }
  function restartAutoplay(){
    clearInterval(testiTimer);
    testiTimer = setInterval(nextTesti, 6000);
  }
  document.getElementById('testiNext')?.addEventListener('click', () => { nextTesti(); restartAutoplay(); });
  document.getElementById('testiPrev')?.addEventListener('click', () => { prevTesti(); restartAutoplay(); });
  testiDots.forEach(dot => {
    dot.addEventListener('click', () => { showTesti(parseInt(dot.dataset.i, 10)); restartAutoplay(); });
  });
  restartAutoplay();

  /* ============================================
     FAQ ACCORDION
     ============================================ */
  const faqItems = document.querySelectorAll('.faq-item');
  function setFaqHeight(item, open){
    const inner = item.querySelector('.faq-a');
    if (open){
      inner.style.maxHeight = inner.scrollHeight + 'px';
    } else {
      inner.style.maxHeight = '0px';
    }
  }
  faqItems.forEach(item => {
    const q = item.querySelector('.faq-q');
    setFaqHeight(item, item.classList.contains('open'));
    q.addEventListener('click', () => {
      const isOpen = item.classList.contains('open');
      faqItems.forEach(other => { other.classList.remove('open'); setFaqHeight(other, false); });
      if (!isOpen){
        item.classList.add('open');
        setFaqHeight(item, true);
      }
    });
  });
  window.addEventListener('resize', () => {
    faqItems.forEach(item => setFaqHeight(item, item.classList.contains('open')));
  });

  /* ============================================
     CONTACT FORM & WHATSAPP REDIRECT
     ============================================ */
  const form = document.getElementById('contactForm');
  document.getElementById("sendBtn")?.addEventListener("click", function(){
    let name = document.getElementById("cName").value;
    let phone = document.getElementById("cPhone").value;
    let email = document.getElementById("cEmail").value;
    let biz = document.getElementById("cBiz").value;
    let msg = document.getElementById("cMsg").value;

    if(!name || !phone){
      alert(document.body.getAttribute('lang') === 'en' ? "Please fill in the required fields (Name & Phone)" : "من فضلك املأ البيانات الأساسية (الاسم والهاتف)");
      return;
    }

    // Meta Pixel Event Tracking for Lead & Contact Form Submission
    if (window.ForsaPixel) {
      window.ForsaPixel.trackContactFormSubmit(biz);
    }

    let text = `📩 طلب جديد من الموقع / New Inquiry:
👤 الاسم / Name: ${name}
📞 الهاتف / Phone: ${phone}
📧 الإيميل / Email: ${email}
🏢 النشاط / Business: ${biz}
📝 التفاصيل / Details: ${msg}`;

    let whatsappNumber = "201101742867"; 
    let url = "https://wa.me/" + whatsappNumber + "?text=" + encodeURIComponent(text);
    window.open(url, "_blank");

    // Form submission feedback
    const btn = document.getElementById("sendBtn");
    const original = btn.textContent;
    btn.textContent = document.body.getAttribute('lang') === 'en' ? 'Sent Successfully ✓' : 'تم الإرسال بنجاح ✓';
    btn.style.opacity = '.85';
    setTimeout(() => {
      btn.textContent = original;
      btn.style.opacity = '1';
      form.reset();
    }, 2600);
  });

  // ── Audio Hero Widget Logic ──
  const heroAudio = document.getElementById('heroAudio');
  const heroPlayBtn = document.getElementById('heroPlayBtn');
  const audioWaves = document.getElementById('audioWaves');
  
  if (heroAudio && heroPlayBtn && audioWaves) {
    const playIcon = heroPlayBtn.querySelector('.play-icon');
    const pauseIcon = heroPlayBtn.querySelector('.pause-icon');

    function updateBtnState() {
      if (heroAudio.paused) {
        playIcon.style.display = 'block';
        pauseIcon.style.display = 'none';
        audioWaves.classList.remove('playing');
      } else {
        playIcon.style.display = 'none';
        pauseIcon.style.display = 'block';
        audioWaves.classList.add('playing');
      }
    }

    heroPlayBtn.addEventListener('click', () => {
      if (heroAudio.paused) {
        document.querySelectorAll('audio, video').forEach(el => {
          if (el !== heroAudio) el.pause();
        });
        heroAudio.play()
          .then(updateBtnState)
          .catch(() => {});
      } else {
        heroAudio.pause();
        updateBtnState();
      }
    });

    const waveBars = audioWaves.querySelectorAll('.wave-bar');

    heroAudio.addEventListener('timeupdate', () => {
      if (heroAudio.duration) {
        const pct = heroAudio.currentTime / heroAudio.duration;
        const activeCount = Math.floor(pct * waveBars.length);
        waveBars.forEach((bar, idx) => {
          if (idx <= activeCount && heroAudio.currentTime > 0) {
            bar.classList.add('active');
          } else {
            bar.classList.remove('active');
          }
        });
      }
    });

    heroAudio.addEventListener('pause', updateBtnState);
    heroAudio.addEventListener('play', updateBtnState);
    heroAudio.addEventListener('ended', () => {
      heroAudio.currentTime = 0;
      waveBars.forEach(bar => bar.classList.remove('active'));
      updateBtnState();
    });
  }

  // Initial translation setup once everything is loaded
  setLanguage(currentLang);

  // ============================================
  // CONTENT PROTECTION (PREVENT COPYING/DOWNLOADING)
  // ============================================

  // Prevent right-click context menu (except on text input fields)
  document.addEventListener('contextmenu', (e) => {
    if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;
    e.preventDefault();
  });

  // Prevent drag and drop of images and videos
  document.addEventListener('dragstart', (e) => {
    if (e.target.tagName === 'IMG' || e.target.tagName === 'VIDEO') {
      e.preventDefault();
    }
  });

  // Prevent copy event
  document.addEventListener('copy', (e) => {
    e.preventDefault();
  });

  // Prevent selectstart event
  document.addEventListener('selectstart', (e) => {
    e.preventDefault();
  });

  // Prevent security bypass keyboard shortcuts (Ctrl+C, Ctrl+S, Ctrl+U, F12)
  document.addEventListener('keydown', (e) => {
    if (e.ctrlKey && (e.key === 'c' || e.key === 'x' || e.key === 's' || e.key === 'u')) {
      e.preventDefault();
    }
    if (e.key === 'F12') {
      e.preventDefault();
    }
  });

  // ── Scroll To Top Button ──────────────────────────────
  const scrollBtn = document.getElementById('scrollToTopBtn');
  if (scrollBtn) {
    window.addEventListener('scroll', () => {
      if (window.scrollY > 300) {
        scrollBtn.classList.add('visible');
      } else {
        scrollBtn.classList.remove('visible');
      }
    }, { passive: true });

    scrollBtn.addEventListener('click', () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
  }

  /* ============================================
     META PIXEL COMPLETE TRACKING SYSTEM
     ============================================ */
  const ForsaPixel = {
    // Safe tracker helper to prevent duplicate/broken execution
    track: function(eventName, params = {}, isCustom = false) {
      if (typeof window.fbq !== 'function') return;
      try {
        if (isCustom) {
          window.fbq('trackCustom', eventName, params);
        } else {
          window.fbq('track', eventName, params);
        }
      } catch (err) {}
    },

    // 2. Portfolio View (ViewContent) & 4. Project Details (ProjectViewed)
    trackPortfolioView: function(item, content) {
      const title = content ? content.title : 'Project';
      const catName = content ? content.cat : (item.category || 'Portfolio');
      const projId = 'pf_' + (item.category || 'item') + '_' + title.replace(/[^a-zA-Z0-9\u0600-\u06FF]/g, '_');

      // Standard ViewContent
      this.track('ViewContent', {
        content_type: 'portfolio',
        content_name: title,
        content_category: catName,
        content_id: projId
      });

      // Custom ProjectViewed
      this.track('ProjectViewed', {
        project_name: title,
        project_category: catName,
        project_id: projId
      }, true);
    },

    // 3. Portfolio Category View
    trackCategoryView: function(filterKey, buttonLabel) {
      const categoryMap = {
        'all': 'All Projects',
        'social': 'Social Media',
        'design': 'Visual Identity & Graphic Design',
        'print': 'Printing',
        'ads': 'Advertising Campaigns',
        'videos': 'Video Editing'
      };
      const catName = categoryMap[filterKey] || buttonLabel || filterKey;
      this.track('PortfolioCategoryView', { category: catName }, true);
    },

    // 11. Contact Form Lead Submission
    trackContactFormSubmit: function(bizType) {
      this.track('Lead', {
        content_name: 'Contact Form Lead',
        business_type: bizType
      });
      this.track('ContactFormSubmit', {
        business_type: bizType,
        name_provided: true
      }, true);
    },

    // Auto Tracking Setup (Scroll, Time, Clicks, Downloads)
    initAutoTracking: function() {
      const self = this;

      // 12. Scroll Depth Tracking (25%, 50%, 75%, 100%) — Fires once per threshold per session
      const firedDepths = new Set();
      const checkScrollDepth = () => {
        const winHeight = window.innerHeight;
        const docHeight = document.documentElement.scrollHeight - winHeight;
        if (docHeight <= 0) return;
        const scrollPct = Math.round((window.scrollY / docHeight) * 100);

        const thresholds = [25, 50, 75, 100];
        thresholds.forEach(t => {
          if (scrollPct >= t && !firedDepths.has(t)) {
            firedDepths.add(t);
            self.track('ScrollDepth', { depth: t + '%' }, true);
          }
        });
      };

      let scrollDebounce;
      window.addEventListener('scroll', () => {
        if (!scrollDebounce) {
          scrollDebounce = setTimeout(() => {
            scrollDebounce = null;
            checkScrollDepth();
          }, 250);
        }
      }, { passive: true });

      // 13. Time On Page Tracking (30s, 60s, 120s) — Fires once per threshold
      const timeThresholds = [30, 60, 120];
      timeThresholds.forEach(sec => {
        setTimeout(() => {
          self.track('TimeOnPage', { seconds: sec }, true);
        }, sec * 1000);
      });

      // Unified Event Delegation (5-10, 14: WhatsApp, FB, IG, Phone, Email, Request Service, Downloads)
      document.addEventListener('click', (e) => {
        // 5. WhatsApp Click
        const waLink = e.target.closest('a[href*="wa.me"], .social-btn.whatsapp');
        if (waLink) {
          const loc = waLink.closest('header') ? 'header' :
                      waLink.closest('footer') ? 'footer' :
                      waLink.closest('.contact') ? 'contact_section' : 'button';
          self.track('Contact', { content_name: 'WhatsApp Click', location: loc });
          self.track('WhatsAppClick', { location: loc }, true);
          return;
        }

        // 6. Facebook Click
        const fbLink = e.target.closest('a[href*="facebook.com"], .social-btn.facebook');
        if (fbLink) {
          const loc = fbLink.closest('footer') ? 'footer' : 'contact_section';
          self.track('FacebookClick', { location: loc }, true);
          return;
        }

        // 7. Instagram Click
        const igLink = e.target.closest('a[href*="instagram.com"], .social-btn.instagram');
        if (igLink) {
          const loc = igLink.closest('footer') ? 'footer' : 'contact_section';
          self.track('InstagramClick', { location: loc }, true);
          return;
        }

        // 8. Phone Call Click
        const phoneCard = e.target.closest('.contact-card');
        const phoneLink = e.target.closest('a[href^="tel:"]') || (phoneCard && phoneCard.textContent.includes('201101742867'));
        if (phoneLink) {
          self.track('PhoneCallClick', { phone_number: '+201101742867' }, true);
          return;
        }

        // 9. Email Click
        const mailLink = e.target.closest('a[href^="mailto:"]');
        if (mailLink) {
          self.track('EmailClick', { email_address: 'info@forsa-media.com' }, true);
          return;
        }

        // 10. Request Service Click
        const srvBtn = e.target.closest('.service-link, [data-i18n="hero-btn-start"], [data-i18n="btn-consult"], [data-i18n="pkg-btn"], [data-i18n="srv-more"]');
        if (srvBtn) {
          const btnText = srvBtn.textContent.trim();
          self.track('Lead', { content_name: btnText || 'Request Service' });
          self.track('RequestService', { button_text: btnText }, true);
          return;
        }

        // 14. File Downloads Tracking (.pdf, .zip, .doc, .docx, or download attr)
        const dlLink = e.target.closest('a[href$=".pdf"], a[href$=".zip"], a[href$=".doc"], a[href$=".docx"], a[download]');
        if (dlLink) {
          const href = dlLink.href || '';
          const name = dlLink.download || href.split('/').pop() || 'File';
          self.track('FileDownload', { file_name: name, file_url: href }, true);
        }
      }, { passive: true });
    }
  };

  window.ForsaPixel = ForsaPixel;
  ForsaPixel.initAutoTracking();

})();
